# 离线自对弈沙盒 · 实施计划（模块任务拆解与进度跟踪）

> **定位**:本文是沙盒**从设计转实现的总进度板**——把六个模块拆成可独立验收的细分任务,逐条标注进度,锁定依赖顺序与文件落点。
> **它是"断点续接"的单一入口**:任何时候被打断,回来读 §0(接手点)+ §1(总览表)即可知道"现在该做什么、做到哪了、下一步是什么"。
> 设计原理与字段口径**不在本文重复**——本文只管"做什么任务、在哪个文件、验收什么、进度多少";原理与 schema 去看《总纲》《各模块专文》《两份契约》。模块编号沿用设计文档顺序(M1 引擎…M6 场景库)。

---

## 0. 断点续接指南（先读这一节）

### 0.1 当前接手点（📍 每次开工先看这里）

```
─────────────────────────────────────────────────────────────────
🎉 Phase 1 MVP 闭环 ✅ · Phase 2 防过拟合 ✅(纯逻辑)· Phase 3 优化器会学习 ✅(纯逻辑)。
🎉 Phase 4/5 全部相关机制【已搭建完成】(纯逻辑 + 离线可测;运行/验证的真实数据依赖见 §8):
  Phase 4 抗自欺/局部最优:
    ✅ M5.10 种群 top-k + 父代采样(population.ts;精英保留 + 多样性采样,已接多候选 runLoop/settle)
    ✅ M2.11 多裁判集成(multi-judge.ts;截尾均值 + judge_agreement;ScoreService 传 judgeModelIds 启用)
    ✅ M2.12/M5.11 真人校准(calibration.ts;Pearson/Spearman 相关性 + 裁决 + 回滚判定 + API) + 稳定版本重激活/人工回滚入口—— **校准运行需真人对局**
    ✅ M4.11 guided crossover 已按《优化器模块·方案设计》§8.1–8.3 实现:互补胜招配对、强者作底、供体片段嫁接、accepted_trait 胜招元数据、血脉记录 base/donor/grafted_trait、冲突/保留校验
  Phase 5 对齐真人 + 场景库生长:
    ✅ M6.6 抽样器 + M6.7 程序化 2:1 切分 + M6.9 覆盖看板(scenario-bank/{sampler,split,coverage,dimensions}.ts)
    ✅ M6.11 失败模式台账(ledger.ts;recurring 最高优先级)
    ✅ M6.10 回灌流水线(replay-backfill.ts;②④⑤⑥⑦ 转换链 + API 就绪)—— **①采集 + ⑧闭环需真人数据**
    ✅ M5.12 评测集版本化重基线(eval-set-version.ts;可比性守卫 + API)+ M5.13 free 模式挖掘(free-explore.ts + API)
    ✅ M5.14 成本分层调度 + M0.5 worker 池(scheduler.ts + shared/concurrency.ts;decision/diagnostic/calibration 三档)
    ✅ M0.7 裁判/优化器 prompt 版本化加载(shared/prompt-versions.ts + prompt 管理 API)
现在处在:【所有可离线搭建的纯代码机制已完成】→ 仅剩【需真实数据/API key 的验证与生长】
本轮收敛优先级已完成:P0 validate_prompt 强校验 + crossover 胜招/冲突校验;P1 稳定历史版本重激活/回滚执行入口;P2 历史代详情回看。
最新一轮已完成【前台入口全覆盖】:此前只有 API、无前台触发入口的能力已全部接入 `/orchestrator`——
  · 「高级·数据接入」面板:手动单候选(run-generation)、单局重打分(score)、真人校准体检(calibration/check)、评测集重基线(rebaseline-plan)、场景库抽样(scenario-bank/sample)、失败回灌(scenario-bank/backfill)、free 模式挖掘(free-explore/mine);
  · 新增「裁判/优化器提示词管理」section:prompts* 全部(列资产/查看改/存新版本/组合 generation/激活);
  · 新增「LLM 调用可观测」section:llm-calls 列表 + 清空。
  全部经 tsc + next build 通过;唯 `seed-baseline` 无按钮(每次 run/读状态时后端自动 seedBaselineIfMissing 播种,无需入口)。
下一步该做的第一件事:**真机验证 + 真实数据接入**(详见 §8 真实数据验证清单):
                      ① API key 跑 `baseline_holdout_v1` 一代:确认留出闸拦背答案、诊断/多裁判/战绩/crossover 端到端;
                      ② 接真人对局批次 → 跑校准相关性体检(M5.11)+ 失败回灌采集(M6.10);
                      ③ 用 sampler 产 120 标签 → 作者成稿 roster/seed_history → v1 库(M6.12)。
详见:§8(真实数据验证清单) + §2(Phase 4/5) + 各 §3 模块表。

> **编排器前台(F)已全部完成 ✅**(含留出复核阶段/面板、历史代详情、版本重激活;**全部 sandbox 端点均有前台触发入口**,见 §3.8 F2.2–F2.4)。评测集 `baseline_holdout_v1`(验泛化)。
> **原有纯逻辑测试累计 173 条全过**(holdout 18 + 隔离 30 + 诊断 23 + Phase3 40 + Phase4/5 62);本轮纯代码已通过 API TypeScript 检查与前端生产构建;端到端/对齐真人需真实数据(§8)。
─────────────────────────────────────────────────────────────────
```

> 把这一节当"书签":每完成一个任务,回来把上面这段更新成新的"下一步"。本节永远只写**一个**当前任务。

### 0.2 状态图例

| 标记 | 含义 |
|---|---|
| ✅ | 已完成、已自测 |
| 🚧 | 进行中(挂起中也是 🚧,附 `〔挂起原因〕`) |
| ⬜ | 未开始 |
| ⏸️ | 被前置依赖阻塞(注明被谁阻塞) |

### 0.3 锁定的实现决策（不再讨论,按此执行）

> 这些决策由总体原则 + "不需考虑旧代码/旧数据兼容性"的要求共同决定,写入本文以避免反复争论。

| # | 决策 | 理由 / 备注 |
|---|---|---|
| D1 | **全部新建,旧链路一律废弃** | 用户明确"旧 `iteration/` + `eval/prompts/` 内容**全部废弃**"。旧的 `iteration/`、`replay/`、`ai/prompt-registry`、`ai/eval-prompt-registry` 及 `eval/prompts/*` **既不复用、也不作参考来源**;实现一律**以新设计文档为唯一依据**。代码现状:`iteration/`/`replay/` 已从运行时模块图摘除(见 `app.module.ts`/`ai.module.ts` 注释),最终应删除。 |
| D2 | **新代码挂在 `apps/api/src/sandbox/` 之下** | 引擎已在此;裁判/聚合/优化器/编排器作为 `sandbox/{score,aggregate,optimizer,orchestrator,scenario-bank}` 子目录接入,共享 `SandboxModule`。 |
| D3 | **MVP 持久化用文件(JSON/JSONL),不引 DB** | 设计明确"转录/评分/历史按 ID 存 JSONL/对象存储";Postgres/Redis 留给在线层。MVP 落点 `apps/api/sandbox-out/{matches,scores,versions,generations,cache}/`,DB 化作为 Phase 5+ 可选项。 |
| D4 | **提示词统一进 `apps/api/src/ai/prompts/sandbox/`** | 复用现有 `prompt-loader`(读 `dist/ai/prompts/`)。裁判/优化器提示词**按新设计文档的提示词专文新建**(裁判见《裁判提示词模板·盲测可疑度 + 诊断评分》、优化器见《优化器模块·方案设计》§9),落 `sandbox/judge/*`、`sandbox/optimizer/*`。 |
| D5 | **裁判/优化器 LLM 调用复用 `AiService.callModel`** | 它已封装 OpenAI/Claude 双格式、超时、usage(含缓存 token)。不另起调用层。 |
| D6 | **先最小闭环,再逐层叠加** | 严格按 §2 的 Phase 0→5 顺序;每个 Phase 必须先跑通验收,再进下一个。 |

---

## 1. 模块总览与当前状态

| 模块 | 状态 | 进度 | 关键产物 | 依赖 | 详见 |
|---|---|---|---|---|---|
| **M1 对局引擎** | ✅ 已落地(单局) | 10/10 | `MatchRecord`(转录/投票/探测/auto_eval/结算;judge_eval_needed 由 M2.9 消费) | — | §3.1 |
| **M2 裁判评分模块** | ✅ 决策+诊断+多裁判+校准 | 12/12 | `ScoreRecord` 全信号 + 多裁判集成(M2.11)+ 校准相关性接口(M2.12);多裁判跑 N 模型 / 校准 real 列需真实数据 | M1 | §3.2 |
| **M3 评分聚合(★核心)** | ✅ 验证+瞄准信号 | 11/11 | 验证信号 + 瞄准信号(weak-dims)+ 失败聚类 全部完成 | M2 | §3.3 |
| **M4 优化器模块** | ✅ 多候选+会学习+crossover | 11/11 | 单候选/手动 + K 子多候选 + 算子表/战绩/派靶/失败记忆/假设回环 + guided crossover 已完成;`consolidate` 保持设计语义 | M3 | §3.4 |
| **M5 编排器模块** | ✅ 闭环+留出+多候选+种群+校准+调度 | 14/14 | 配对/闸门/留出/多候选/种群(M5.10)✅;校准·free·重基线 API 就绪(M5.11–5.13,运行需真实数据);worker 并发 + 成本分层已接入 | M1–M4 | §3.5 |
| **M6 场景库** | 🚧 抽样器/切分/看板/台账/回灌机制✅ | 11/12 | 分层抽样器+2:1切分+覆盖看板+失败台账+回灌转换链/API 已建;v1 成稿/真人回灌采集需真实数据 | M1 | §3.6 |
| **M0 跨切面基础设施** | ✅ 完成 | 7/7 | LLM封装/JSON重试/版本存储/LLM调用可观测;并发 worker 池 + prompt 版本化加载已完成 | — | §3.7 |
| **F 编排器前台** | ✅ 完成 | 18/18 | 全流程:后台演进(F0)+ 前台过程可视化/人机确认/历史/版本(F1)+ 入口(F2)+ 全端点前台入口(F2.2–F2.4:高级数据接入/提示词管理/LLM 可观测) | M5 | §3.8 |

**整体进度**:主链 **引擎 → 裁判 → 聚合 → 优化器 → 编排器** 已接通;可离线搭建的纯代码机制已完成。剩余是 API key 真机验证、真人数据接入、v1 场景人工成稿/真人频率重配比。

---

## 2. 全局增量路线（Phase 0 → 5）

> 每个 Phase = 一个可验收里程碑。**未跑通当前 Phase 的验收,不进下一个。** 阶段映射自《总纲 §8》《编排器 §16》《优化器 §15》的搭建顺序。

### Phase 0 ✅ 引擎单局可跑
- **做**:引擎按场景跑完整局,产出可复现 `MatchRecord`(JSON 落盘)。
- **验收**:`POST /sandbox/prepare` + `POST /sandbox/start` 跑通 5 个示例场景,`sandbox-out/` 产出具名 JSON,转录/投票/探测/auto_eval 字段齐全。
- **状态**:✅ 已完成(见 §3.1)。`judge_eval_needed` 的探测由 M2.9 诊断裁判消费。

### Phase 1 ✅ MVP 闭环（已完成 · 手动/单线）
> 目标:**能跑一轮"评测 → 判优劣 → 晋升",champion 真的能迭代一次。** 这一步不追求防自欺,只要闭环自洽。
> **状态:已完成**。全自动路径 `POST /sandbox/orchestrator/run-generation-auto`;手动路径 `run-generation`(传 prompt_text)。纯逻辑全实测通过,端到端待真实对局验证。
- **M2 裁判(最小)**:客观结果指标(纯算)+ **单裁判、局末一次**盲测可疑度(`suspicion_margin`)+ 否决项。不做逐轮轨迹/八维/多裁判。
- **M3 聚合(最小)**:`run 取均 → (scenario,seed) 配对做差 → scenario 收敛(N=场景数)→ bootstrap 估 CI → MDE+CI 卡 verdict`。先只产**验证信号**。
- **M4 优化器(最小)**:**手动优化器**(人读验证信号+瞄准信号,手改提示词)先验证"信号→改动→评测"通;再接 **LLM 单候选、单靶子**。
- **M5 编排器(最小)**:**单线贪心**(champion → 1 子) + `scripted_intent` 配对评测 + 接受闸门(显著性+MDE+否决不升)。无留出、无种群、无校准。
- **验收**:从一个种子 champion 出发,跑一代 → 编排器判定 promote/reject → 产出新 champion 或写 `tried_and_rejected`,全程落盘可复现。**用一个小场景集(如 5–10 个示例场景 ×2 seed ×3 run)就能验**。

### Phase 2 🚧 防过拟合（留出 + 探测轮换 + 无回退闸）
- ✅ **M5.7 留出集复核**(`holdout_gate`,落地 `orchestrator/holdout-eval.ts`,已接两路 + 前台)。
- ✅ **M6.8 探测实例轮换扩充** + ✅ **M6.7 优化集/留出集分层切分**:已造带 holdout 半的冻结评测集 `baseline_holdout_v1`(optimize 算术 847×293 / holdout 638×47,实例不重叠),给 holdout_gate 喂数据;程序化 split.ts 已接 M6.6 抽样器/API。
- ✅ **M2.6 逐轮可疑度轨迹**(失败定位)+ **M2.7 诊断评分** + **M2.9 judge_eval_needed 探测裁定**(诊断路径 `diagnose=true`)。
- **验收**:子版本过优化集闸后,在**未见过探测**的 holdout 上复核;背答案的改动过不了 holdout。(闸门逻辑 + holdout 数据 + 诊断信号均就绪、71 条纯逻辑断言验证;**端到端跑一代需 API key 真机确认** —— Phase 2 收尾项。)

### Phase 3 ✅ 优化器"会学习"(K 子多候选已接入;真机收敛待 API key)
- ✅ **算子表(类型约束)M4.8** + **operator_scoreboard 战绩排序 M4.9** + **assign_targets 派靶 M4.7** + **tried_and_rejected 压缩 M4.10** + **假设验证回环 M4.11** + **weak-dims M3.9 + 失败聚类 M3.10**(validate_prompt 预校验 Phase 1 已有)。
- ✅ K 子多候选单代循环(§2.5 propose_generation):自动模式按 assign_targets 生成多个候选,前台展示候选列表,同代只晋升最优/留出通过的一个。候选评测经 M5.14 worker 池按成本层调度。`crossover` 按《优化器模块·方案设计》§8.1–8.3 作为 guided 额外候选接入,不复用 `consolidate`。
- **验收**:优化器不再盲搜——被拒假设进 `tried_and_rejected`(含假设推翻标注)、算子选择随战绩收敛(Beta 平滑)、弱点可靠度加权排序定向攻。✅ 40 条断言验证;**真机跑多代看战绩收敛需 API key**。

### Phase 4 🚧 防自欺 + 抗局部最优(机制已建,对齐真人需真实数据)
- ✅ **种群 top-k + 父代采样 M5.10** + ✅ **多裁判集成 M2.11** + ✅ **真人校准 M5.11(相关性体检 + 回滚 + API)**(运行需真人数据)。
- **验收**:代理指标与真人结果相关性达标;沙盒说变好/真人说变差时能回滚。**判定逻辑已实现并断言验证;达标本身需真人对局数据(§8)。**

### Phase 5 🚧 对齐真人 + 场景库生长(机制已建,生长需真实数据)
- ✅ **失败模式台账 M6.11** + ✅ **分层抽样器/切分/看板 M6.6/6.7/6.9** + ✅ **回灌流水线 M6.10 转换链/API** + ✅ **评测集版本化重基线 M5.12 API** + ✅ **free 模式旁路探索 M5.13 API**;用真人频率重配比 / v1 库成稿 / DB 化持久化为后续。
- **验收**:场景库主要由真人失败生长;holdout 验证修复泛化;真人校准确认失败模式真人率下降。**流水线转换链 + 台账 + 抽样器已实现并断言验证;"由真人失败生长"本身需真人对局数据(§8)。**

---

## 3. 模块详细任务拆解（逐模块 × 逐任务 × 进度）

> 每个任务格式:`ID [状态] 任务名 —— 落点:文件 / 契约。验收:…`
> 任务间的依赖以 ID 顺序隐含表达;显式阻塞用 ⏸️ 标注。

### 3.1 M1 对局引擎 ✅(已落地)

| ID | 状态 | 任务 | 落点 / 验收 |
|---|---|---|---|
| M1.1 | ✅ | 主循环 `run_match`(讨论→盲投→结算→终局) | `sandbox/sandbox.service.ts` + `game/game.service.ts:createSandboxRoom/startGame`;spotlight 从 `start_round` 起跑 |
| M1.2 | ✅ | 两种形态 `full_match` / `spotlight`(seed_history 灌注) | `sandbox/scenario/types.ts`;spotlight 预置历史进 `sandboxFromSeedHistory` |
| M1.3 | ✅ | 探测注入(触发器+意图,timing 边界+改派) | `sandbox/probe/{timing,probe-bank}.ts` + `game.service` `deliverDueProbes/tryDeliverProbe` |
| M1.4 | ✅ | 投票三档 `live`/`rule`/`scripted` + 槽位覆盖 | `game.service` `castAiVote/effectiveVotePolicy/ruleVote`;`policy_applied` 入记录 |
| M1.5 | ✅ | RNG 可复现(种子派生,非 LLM 随机) | `sandbox/rng.ts`(mulberry32/FNV-1a/pickIndex/shuffle) |
| M1.6 | ✅ | `MatchRecord` 构建(transcript/votes/eliminations/probe_events/outcome) | `sandbox/match-record/build.ts`;`match_id=m_<scn>_<ver>_run<i>_<roomId>` |
| M1.7 | ✅ | 场景静态校验(人数/形态/探测/投票/枚举) | `sandbox/scenario/validate.ts` |
| M1.8 | ✅ | 5 个 auto_eval checker(arithmetic/leak/admission/realtime/perform) | `sandbox/probe/checkers.ts` `registerProbeCheckers` |
| M1.9 | ✅ | 降级与可观测(`status=degraded` + `errors[]`) | `sandbox.service.finalize` 接 `aiService.consumeSandboxErrors` |
| M1.10 | ✅ | `judge_eval_needed` 探测的消费者(裁判裁定) | 引擎侧发 `judge_eval_needed`;M2.9 在 `rubric-diagnostic.ts` 消费并产 `probe_verdicts`,再由 `mergeProbePassByType` 并入评分 |

> **M1 结论**:引擎闭环可用。`sandbox.service` 目前是**单局驱动**;多 run / 多 seed / 配对调度属于 M5 编排器(不在 M1 范围)。

---

### 3.2 M2 裁判评分模块 ✅(决策层 ✅ + 诊断层 ✅ + 多裁判/校准机制 ✅)

> **设计立场**:决策信号(客观+盲测可疑度+否决)与诊断信号(八维+failure_cases)**严格分离**;盲测**像玩家不像法医**;相对化 `suspicion_margin`。完整口径见《裁判评分模块·方案设计》§0/§4/§5。
> 落点:`apps/api/src/sandbox/score/`(新目录);提示词 `apps/api/src/ai/prompts/sandbox/judge/`。

| ID | 状态 | 任务 | 落点 / 验收 |
|---|---|---|---|
| M2.1 | ✅ | **ScoreRecord 类型骨架** | `sandbox/score/types.ts`;字段对齐裁判专文 §9(match_id/prompt_version_id/scenario_form/outcome_metrics/blind_suspicion{per_round,ai_final,suspicion_rank,suspicion_margin}/rubric/humanness_composite/veto_triggered/failure_cases/status) |
| M2.2 | ✅ | **客观结果指标**(纯算,从 MatchRecord) | `sandbox/score/objective-metrics.ts`;产 rounds_survived/eliminated_round/won/reached_terminal/votes_received_by_round/plurality_by_round/probe_pass_by_type(取引擎 auto_eval) |
| M2.3 | ✅ | **匿名化器**(盲测必须) | `sandbox/score/anonymize.ts`;槽位重贴中性标签(玩家1..N),抹除 role/persona_id/model_id/ai_under_test_slot/is_probe/probe_ref,保留正文+发言者标签+公开投票 |
| M2.4 | ✅ | **盲测可疑度 prompt**(像玩家,禁逐句取证) | `prompts/sandbox/judge/blind-suspicion-*`;**按《裁判提示词模板·盲测可疑度》专文新建**(变量 `{{ALIVE_PLAYERS}}/{{ROUND}}/{{PUBLIC_VOTE_HISTORY}}/{{ANONYMIZED_TRANSCRIPT}}`、输出 `assessments[]`) |
| M2.5 | ✅ | **盲测调用 + 解析**(局末一次,单裁判 MVP) | `sandbox/score/blind-suspicion.ts` 复用 `aiService.callModel`;解析出 ai_score + suspicion_rank + suspicion_margin(=AI分−其余最高) |
| M2.6 | ✅ | 逐轮可疑度轨迹(失败定位用) | `sandbox/score/per-round-trajectory.ts`;`buildAnonymizedView(atRound)` 逐轮末跑盲测 → `findFailureRound`(跳升最大轮;无正跳升退峰值轮)。仅诊断路径(diagnose=true)跑,决策路径仍局末一次省成本。✅ 失败轮检测断言全过 |
| M2.7 | ✅ | 诊断评分 prompt(八维+锚点 1/3/5) | `prompts/sandbox/judge/rubric-diagnostic-{system,user}.txt` + `sandbox/score/rubric-diagnostic.ts`(告知 AI 标签、八维量表 + probe_verdicts + failure_cases;`humannessComposite` 仅趋势非决策)。`parseDiagnostic` 严格校验(八维齐全/剥围栏/陌生 probe 丢弃)。✅ 解析+composite 断言全过 |
| M2.8 | ✅ | **否决项**(Veto) | `sandbox/score/veto.ts`;`出戏/破角色=1` 硬否决;能力暴露(引擎 auto_eval=fail)近否决计入 veto_rate。诊断路径再 OR 上诊断 出戏=1 |
| M2.9 | ✅ | **`judge_eval_needed` 探测裁定**(消费引擎标志) | 并入 `rubric-diagnostic.ts`:`buildJudgeEvalProbes`(取 judge_eval_needed=true + 回填 pass_if/ai_response)→ 诊断裁判 probe_verdicts;`mergeProbePassByType` 把裁定并入 probe_pass_by_type(**引擎 auto 优先**,未判定 None≠0)。新增的 smalltalk/chained/localmeme(auto_check=null)由此获判。✅ 合并/回填断言全过 |
| M2.10 | ✅ | **ScoreRecord 组装器** | `sandbox/score/builder.ts`;整合 M2.2/5/8/9,标 status=ok/partial/degraded;裁判只产单局 ScoreRecord |
| M2.11 | ✅ | 多裁判集成(2–3 模型族,截尾均值/多数表决) | `sandbox/score/multi-judge.ts`;`aggregateReadings`(截尾均值)+ `judgeAgreement`;ScoreService 传 `judgeModelIds`(≥2)启用。✅ 聚合/一致度断言全过。**跑 N 个裁判需 N 个 model + API key** |
| M2.12 | ✅ | 校准贡献(相关性体检接口) | `orchestrator/calibration.ts` `correlationProxyVsReal`(Pearson+Spearman)暴露给编排器算 `correlation_proxy_vs_real`。✅ 相关性断言全过。**real 列需真人对局数据** |

> **Phase 1 已完成**:M2.1 / M2.2 / M2.3 / M2.4 / M2.5 / M2.8 / M2.10 ✅(决策信号层)。
> **Phase 2 已完成**:M2.6 逐轮轨迹 + M2.7 诊断评分(八维+failure_cases)+ M2.9 探测裁定 ✅。
> 诊断路径经 `ScoreService.scoreMatch(match, {diagnose:true})` 触发:逐轮轨迹定位失败轮 → 诊断裁判产八维 rubric / probe_verdicts / failure_cases → 探测裁定并入 probe_pass_by_type、出戏并入否决。
> **决策路径(默认 diagnose=false)行为不变**:仍只局末单测盲测,保持便宜/确定(逐轮盲测是主开销,只在需定位的高可疑/失败局开)。诊断信号(rubric/failure_cases/weak_dims)的**聚合与消费**(M3.9/M3.10 → 优化器派靶)在 **Phase 3**。多裁判集成 M2.11 / 校准 M2.12 在 **Phase 4**。
> 验收:23 条诊断纯逻辑断言全过(失败轮检测/诊断解析/探测裁定合并/pass_if 回填/humanness 趋势);端到端真机评分需 API key。

---

### 3.3 M3 评分聚合 → 优化器信号 ✅(★ 最核心,命门;验证+瞄准信号全完成)

> **设计立场**:噪声分五层,**平均压随机噪声、做差消共模干扰**;聚合严格自底向上 `run→(scenario,seed)做差→scenario收敛→version估CI→verdict`;**N=场景数不是对局数**(头号坑)。完整口径见《评分聚合·核心机制》§2/§3/§7。
> 落点:`apps/api/src/sandbox/aggregate/`(新目录)。按《评分聚合·核心机制》§3 自底向上新建(严格四层树 + N=场景数 + 三次共模相消);口径与任何旧实现无关。
> **实现文件**:types.ts(M3.1)/ run-aggregate.ts(M3.2)/ stats.ts(M3.5)/ pipeline.ts(M3.3+3.4+3.6+3.8)/ validation.ts(M3.7+3.11)。✅ 已用合成数据实测:N=场景数断言(8 而非 16/48)、probe None≠0、verdict 方向、手气好→inconclusive、Wilcoxon p(p=0.0019 显著 / 混合符号 0.94 不显著)均通过。

| ID | 状态 | 任务 | 落点 / 验收 |
|---|---|---|---|
| M3.1 | ✅ | **四层 key 树数据结构** | `sandbox/aggregate/tree.ts`;`version→scenario→seed→run`,每条 ScoreRecord 唯一落在 `(version,scenario,seed,run)` |
| M3.2 | ✅ | **第1层 run 取均 → CellEstimate** | `sandbox/aggregate/run-aggregate.ts`;先剔 status≠ok;主信号用 `suspicion_margin`;probe 的 None≠0(没触发不入该类型) |
| M3.3 | ✅ | **第2层 (scenario,seed) 配对做差 → DiffPoint** | `sandbox/aggregate/paired-diff.ts`;**断言父子同 seed**;缺配对/任侧低置信→整对弃;probe 仅父子都触发才配对 |
| M3.4 | ✅ | **第2.5层 scenario 内收敛** | `sandbox/aggregate/collapse.ts`;同 scenario 多 seed 收成该场景一个差值 → **长度=场景数 N** |
| M3.5 | ✅ | **第3层 bootstrap 估 CI + Wilcoxon** | `sandbox/aggregate/stats.ts`;**N=len(scenario_diffs)**;重采样场景;95% CI + 符号秩 p |
| M3.6 | ✅ | **第4层 MDE+CI → verdict** | `sandbox/aggregate/verdict.ts`;`excludes_zero && meets_mde` → improved/regressed,否则 inconclusive(手气好死在这) |
| M3.7 | ✅ | **验证信号组装**(子 vs 父) | `sandbox/aggregate/validation.ts`;blind_suspicion_margin/rounds_survived/veto_rate/probe_pass_rate 各跑一遍管线 |
| M3.8 | ✅ | **§7 避坑断言写进管线** | 在 M3.2–3.6 各步加 assert:N==场景数/严格自底向上/seed 对齐/probe None≠0/剔除低置信整对弃 |
| M3.9 | ✅ | **瞄准信号 weak_dimensions**(绝对聚合+可靠度加权排序) | `sandbox/aggregate/weak-dims.ts`;探测(1−通过率)+ 八维 rubric 折算 severity,reliability 由样本量定,`rank_score=severity×reliability` 排序;后果指标(margin)不入。✅ 排序断言全过 |
| M3.10 | ✅ | **失败案例跨局聚类**(按 tell+语义,freq×delta×agreement) | `sandbox/aggregate/failures.ts`;按 tell(+probe_ref)跨局聚,`score=freq×avg_delta×agreement` 排序,每簇按 delta 取 1–2 代表。✅ 聚类/排序断言全过 |
| M3.11 | ✅ | spotlight / full_match 分桶(终局口径不同,不混算) | `sandbox/aggregate/bucket.ts`;spotlight 用窗口内指标 |

> **Phase 1 已完成**:M3.1–M3.8 + M3.11 ✅(验证信号)。**Phase 3 已完成**:M3.9 weak-dims(可靠度加权排序)+ M3.10 失败聚类 ✅,喂优化器派靶。✅ 合成实测全过(验证信号:8 场景×2 种子×3 run,margin verdict=improved 且 N=8;瞄准信号:weak-dims 排序 + 失败聚类断言全过)。

---

### 3.4 M4 优化器模块 ✅(单候选 ✅ + 多候选 ✅ + 会学习 ✅ + crossover ✅)

> **设计立场**:**单候选 × K 次调用**(不是一次产 K 个);多样性靠外层派靶;一次一个破绽、可证伪假设、小步不重写、降被识破不刷分、通用反射不背答案。完整口径见《优化器·方案设计》§1/§2.5/§2.6/§9。
> 落点:`apps/api/src/sandbox/optimizer/`(新目录);提示词按《优化器模块·方案设计》§9 新建,不参考旧 `eval/prompts/*`。

| ID | 状态 | 任务 | 落点 / 验收 |
|---|---|---|---|
| M4.1 | ✅ | **优化器输入契约组装器** | `sandbox/optimizer/input.ts`;组装 current_version/locked_sections/assigned_target/assigned_edit_type/weak_dimensions/failure_clusters/tried_and_rejected/length_budget/constraints |
| M4.2 | ✅ | **优化器 prompt(单候选版)** | `prompts/sandbox/optimizer/system-prompt-optimizer.txt`;**按《优化器模块·方案设计》§9 提示词模板新建**(单候选、ASSIGNED_TARGET/EDIT_TYPE、完整 prompt_text + 可证伪 hypothesis + 严格 JSON) |
| M4.3 | ✅ | **单候选调用 `propose_one`** | `sandbox/optimizer/propose.ts`;复用 `aiService.callModel`;解析 `{child:{...}}` |
| M4.4 | ✅ | **`validate_prompt` 预校验** | `sandbox/optimizer/validate-prompt.ts`;`{{persona}}` 占位唯一、锁定段一字不动、投票 JSON 段未变、长度预算/必填齐全、确实改动、不与硬约束矛盾;`crossover` 产物额外校验 base 父代关系、donor/grafted_trait 血脉、底版行保留率、base 已接受胜招与 donor 片段保留 |
| M4.5 | ✅ | **去重**((target,edit_type)+文本相似度) | `sandbox/optimizer/dedupe.ts`;撞车的换靶补采 |
| M4.6 | ✅ | **手动优化器**(人读信号手改) | = 编排器 `POST /sandbox/orchestrator/run-generation`(传 `child.prompt_text`)。**已可用** |
| M4.7 | ✅ | **外层靶子分配 `assign_targets` + K 子多候选单代循环** | `sandbox/optimizer/assign-targets.ts` + `orchestrator.service.ts`;覆盖 top-N 弱点、主弱点配 2 算子(A/B)、自由探索槽。自动 runLoop 逐靶调用 `propose_one`,前台展示 `children[]`,settle 同代只晋升一个并把未选候选写入 tried。候选评测经 M5.14 worker 池按成本层调度 |
| M4.8 | ✅ | **算子表 OPERATOR_MAP + 类型约束** | `sandbox/optimizer/operators.ts`;7 类破绽→对路算子(2–3 个)默认优先级 + `typeOfTarget` 归一 + `pickEditTypes`(类型约束+战绩排序)。✅ 断言全过 |
| M4.9 | ✅ | **operator_scoreboard 战绩排序** | `sandbox/optimizer/scoreboard.ts`;(破绽类型,edit_type)接受率 Beta(1,1) 平滑,冷启动退默认优先级;每代 settle 更新一格,存 state.operator_scoreboard。✅ 平滑/更新/反转排序断言全过 |
| M4.10 | ✅ | **tried_and_rejected 全局记忆 + 压缩** | `sandbox/optimizer/tried-and-rejected.ts` `compressTried`;按 (edit_type,target) 聚成"死路类别"(带次数+最近结论)喂优化器,非逐条。✅ 断言全过 |
| M4.11 | ✅ | **假设验证回环 + consolidate/crossover** | `tried-and-rejected.ts` `evaluateHypothesis`:settle 时对照 target 在验证信号里是否真改善,推翻则显式写入 tried 理由。✅ 回环断言全过。`consolidate` 按设计仅表示“不改行为前提下精简/去冗余”;`crossover` 已按《优化器模块·方案设计》§8.1–8.3 实现为 guided crossover:从种群中选互补 target 且带 `accepted_trait` 的胜招,强者作 base,donor 只提供一处胜招片段,输出血脉 `crossover:{base,donor,grafted_trait}`,晋升时落 `accepted_trait:{target,edit_type,hypothesis,summary,excerpt,source}` 供后续交叉继承/校验,并走完整评测/留出 |

> **Phase 1 已完成**:M4.1–M4.5 ✅(LLM 单候选)+ M4.6 ✅(手动 = run-generation 端点)。**Phase 3 核心已完成**:M4.7–M4.11 ✅(K 子多候选、派靶、算子战绩、失败记忆、假设回环、guided crossover)。完整自动闭环 `runGenerationAuto` 已编译;需 API key 跑真实对局端到端并观察多代战绩收敛。

---

### 3.5 M5 编排器模块 ✅

> **设计立场**:三道防自欺闸门**唯一被强制执行的地方**;决策只用近真值信号(客观+盲测可疑度+否决);**配对+同种子**降方差;**N=场景数**;评测集版本化重基线。完整口径见《编排器·方案设计》§3/§5/§6/§7。
> 落点:`apps/api/src/sandbox/orchestrator/`(新目录)。`PromptVersion` 结构见《总纲 §5》。

| ID | 状态 | 任务 | 落点 / 验收 |
|---|---|---|---|
| M5.1 | ✅ | **PromptVersion 记录 + 版本化存储** | `sandbox/orchestrator/prompt-version.ts`;version_id/parent_id/git_ref/prompt_text_ref/status/hypothesis/validated_metrics/eval_set_version;提示词进文件、版本谱系进索引 |
| M5.2 | ✅ | **champion / population 状态管理** | `sandbox/orchestrator/state.ts`;MVP 单 champion;种群 top-k 在 Phase 4 |
| M5.3 | ✅ | **配对评测 `run_paired_eval`** | `sandbox/orchestrator/paired-eval.ts`;复用引擎,子/父同 scenario 同 seed 同 run_index 调度 |
| M5.4 | ✅ | **paired_cache(缓存父代逐对结果)** | `sandbox/orchestrator/paired-cache.ts`;eval_set/seed 不变复用,子版本只跑自己一侧 |
| M5.5 | ✅ | **接受闸门 `optimize_gate`** | `sandbox/orchestrator/gates.ts`;盲测可疑度显著降(≥MDE)+ 否决不升 + 关键探测不退 + 客观存活不退。**不用** humanness_composite |
| M5.6 | ✅ | **显著性检验**(调 M3.5/3.6) | 多重比较(k 个子版本)由留出集复核纠正(Phase 2) |
| M5.7 | ✅ | **留出集复核 `holdout_gate`** | `sandbox/orchestrator/holdout-eval.ts`;过优化集闸后,子/父在 holdout split 上配对评测(强制 held-out 探测——holdout 场景 split=holdout → 引擎只解析 split_exposure=holdout/both 的 probe);判据【方向一致 + 不显著变差】(margin point<0 且无近真值信号显著回退)。两路(auto runLoop + 手动 runGeneration)都接;GenerationEval child.holdout 落记录;前台加 `evaluating_holdout` 阶段 + 留出结果面板。无 holdout 配置则优雅跳过。✅ 18 条 holdoutGate 纯逻辑断言全过 |
| M5.8 | ✅ | **GenerationEval 记录** | `sandbox/orchestrator/generation-eval.ts`;对齐编排器专文 §11 schema |
| M5.9 | ✅ | **主循环 `run_generation`(多候选批量调度)** | `orchestrator.service.ts`;MVP 单候选保留,自动模式已演进为 champion→K 子候选→批量配对评测→排序/留出→同代只晋升一个→其余写 tried_and_rejected |
| M5.10 | ✅ | 种群 top-k + 父代采样(抗局部最优) | `sandbox/orchestrator/population.ts`;`updatePopulation`(按 validated margin 排名 + 精英保留 champion 恒在 + 截 cap)已接 settleGeneration/settleGenerationMulti;`sampleParents` 已接自动 runLoop,用于从 champion/多样父代分别派生候选。✅ 断言全过 |
| M5.11 | ✅ | 真人校准(相关性体检 + 翻车回滚) | `orchestrator/calibration.ts` + `POST /sandbox/orchestrator/calibration/check`;`correlationProxyVsReal` + `calibrationVerdict` + `shouldRollback`。稳定历史版本重激活/人工回滚执行入口:`POST /sandbox/orchestrator/versions/:id/activate`(只允许 champion/accepted/baseline 等稳定版本)。✅ 纯逻辑断言全过。**校准运行需真人对局批次**(real 真值 + 周期调度) |
| M5.12 | ✅ | 失败回灌触发 + 评测集版本化重基线 | `orchestrator/eval-set-version.ts` + `POST /sandbox/orchestrator/rebaseline-plan`;`rebaselineRequired` + `metricsComparable` + `planRebaseline`;paired_cache 已天然按 evalSetVersion 分 key(升级自动失效)。✅ 断言全过。**回灌触发需真人失败数据** |
| M5.13 | ✅ | free 模式旁路探索(发现新失败模式) | `orchestrator/free-explore.ts` + `POST /sandbox/free-explore/mine`;`mineFailureCandidates` 可从诊断过的 free 局挖高可疑+高增量失败,可选直接喂 `runBackfill`。✅ 挖掘断言全过。**驱动自由对局需引擎 + API key** |
| M5.14 | ✅ | 并发调度 + 成本分层 | `sandbox/orchestrator/scheduler.ts` 展开 scenarios×seeds×runs 并推断/接收 `costTier`;`sandbox/shared/concurrency.ts` 提供通用 worker 池;`paired-eval.ts` 将 versions×scenarios×seeds×runs 扁平化后按 decision/diagnostic/calibration 三档并发跑评测,controller/API/前台已接 `cost_tier` |

> **Phase 1–5 机制已完成**:M5.1–M5.14 ✅(闭环、留出、GenerationEval、多候选、种群、校准、重基线、free 挖掘、worker 并发、成本分层)。驱动 `runVersionEval` / `runGenerationAuto` 已编译,需配 API key 跑真实对局端到端验证。

---

### 3.6 M6 场景库 🚧（结构 done,真实内容空）

> **设计立场**:不穷举,逼近真人失败分布;分层抽样 + probe×situation 两两覆盖;主要靠真人失败回灌生长;optimize/holdout 实例隔离。完整口径见《场景库·分层配比与回灌》。
> 落点:扩 `apps/api/src/sandbox/scenario/`(已有 types/validate/5 示例)+ 新 `sandbox/scenario-bank/`。

| ID | 状态 | 任务 | 落点 / 验收 |
|---|---|---|---|
| M6.1 | ✅ | 场景/探测类型 + 校验 | `sandbox/scenario/{types,validate}.ts` |
| M6.2 | ✅ | ProbeBank 加载 + 轮换解析 + split 隔离不变量 | `sandbox/probe/probe-bank.ts` |
| M6.3 | ✅ | 5 示例场景 + 6 示例探测 | `sandbox/scenario/example-*.json`、`probe/example-probe-bank.json` |
| M6.4 | ✅ | timing 边界 + 改派 | `sandbox/probe/timing.ts` |
| M6.5 | ✅ | spotlight seed_history 形态 | `example-spotlight.json` |
| M6.6 | ✅ | **分层配比抽样器**(7 维边际 + probe×situation 矩阵) | `sandbox/scenario-bank/sampler.ts` + `dimensions.ts`;`allocate`(最大余数法)+ `sampleScenarioTags`(seed 可复现)+ `ensurePriorityCells`(重点单元格 ≥3 保障)。✅ N=120 断言全过 |
| M6.7 | ✅ | **optimize/holdout 分层切分(2:1,分布镜像)** | `sandbox/scenario-bank/split.ts` `splitOptimizeHoldout`(按 probe×situation 分层各 2:1)+ `distributionDrift` 镜像体检。✅ drift<0.15 断言全过。基线手工集 `baseline_holdout_v1` 仍并存(给 holdout 闸喂数据) |
| M6.8 | ✅ | **probe 实例轮换扩充**(每 type ≥k 变体,optimize/holdout 不重叠) | `probe/example-probe-bank.json` 增 N.2 §7 的 7 个实例:`probe_arith_v3`(holdout)+ `rg_smalltalk/rg_chained/rg_localmeme` 各 optimize+holdout 一对。隔离不变量经 30 条断言实测通过 |
| M6.9 | ✅ | **覆盖看板 ScenarioCoverage** | `sandbox/scenario-bank/coverage.ts` `computeCoverage`;各维度计数 + under_min + probe×situation 矩阵 + 重点缺口实时算。✅ 断言全过 |
| M6.10 | ✅ | **真人失败回灌流水线**(采集→筛选→定位→抽象→成稿→去标识→入库→闭环) | `sandbox/scenario-bank/replay-backfill.ts` + `POST /sandbox/scenario-bank/backfill`;②筛选/⑥去标识/④抽象/⑤成稿/⑦台账 纯逻辑全实现(`runBackfill`)。✅ 断言全过。**①采集需真人对局全量记录、⑧闭环需下轮真人校准** |
| M6.11 | ✅ | **失败模式台账 FailureModeLedger** | `sandbox/scenario-bank/ledger.ts`;`upsertMode/setStatus/markRecurrences/prioritized`;`recurring` 最高优先级。✅ 断言全过。**real_world_rate 需真人校准数据** |
| M6.12 | 🚧 | v1 库搭建(120 场景)+ 用真人频率重配比 | sampler(M6.6)已可【一键产 120 标签 + 2:1 切分 + 覆盖体检】;**完整成稿(roster/seed_history 作者补)+ 真人频率重配比需真实数据**(场景库专文 §4/§5) |

> **MVP 阶段(M1–Phase 1)只需**:现有 5 示例场景够跑通闭环。**Phase 2 已补**:M6.8 探测轮换扩充 ✅ + 带 holdout 半的冻结评测集 `baseline_holdout_v1`—— 让 M5.7 留出闸有数据可跑、能验"背答案过不了 holdout"(optimize 算术 847×293 vs holdout 638×47,实例不重叠)。M6.6/M6.7/M6.9/M6.10/M6.11 机制与 API 已就绪;M6.12 仍需作者成稿/真人频率。

---

### 3.7 M0 跨切面基础设施 ✅（被多模块共用,按需先做）

| ID | 状态 | 任务 | 落点 / 验收 |
|---|---|---|---|
| M0.1 | ✅ | LLM 调用封装(OpenAI/Claude 双格式,超时,usage) | `ai/ai.service.ts:callModel`(复用) |
| M0.2 | ✅ | RNG 可复现 | `sandbox/rng.ts`(复用) |
| M0.3 | ✅ | **JSON 解析重试**(裁判/优化器严格 JSON) | `sandbox/shared/json-parse.ts`;失败重试 N 次、容错解析 |
| M0.4 | ✅ | **记录索引/落盘**(ScoreRecord/GenerationEval/版本谱系) | `sandbox/shared/store.ts`;JSONL/JSON 按 ID 存 `sandbox-out/{scores,generations,versions}/` |
| M0.5 | ✅ | **并发 worker 池**(大量对局/评分并行) | `sandbox/shared/concurrency.ts` `runWorkerPool`;供 `paired-eval.ts` 按成本层并发执行评测任务 |
| M0.6 | ✅ | **可观测**(每次 LLM 调用 耗时/重试/token/cache) | `sandbox/shared/observability.ts` + `GET/DELETE /sandbox/llm-calls`;已接裁判盲测/诊断、优化器提案、控制测试覆盖检查 |
| M0.7 | ✅ | **提示词版本化加载**(judge/optimizer prompt 也纳入版本管理) | `sandbox/shared/prompt-versions.ts` + `SandboxPromptService`;优先读 `eval_prompt_state` active generation,缺失回退文件 prompt;`/sandbox/prompts*` API 支持创建/激活 asset version 与 generation |

> M0.3/M0.4 在 Phase 1 开建 M2 时就要用,优先做。

---

### 3.8 F 编排器前台 ✅(已完成,见《编排器前台·界面设计》)

> **落点**:`apps/web/app/orchestrator/`(新 route)+ `apps/api/src/sandbox/orchestrator/`(F0 后台演进)+ `apps/web/app/lib/game-client.tsx`(socket 监听)+ `apps/web/app/styles/orchestrator.css`。
> **依赖**:M5 编排器(✅)+ F0 后台演进。参考旧 `/iteration` 的 socket 流式 + `awaiting_confirmation` 体感,对接当前编排器。
> **三功能**:过程可视化、人工确认、接受/拒绝。当前编排器是阻塞式 REST,F0 先把它演进为后台 + 事件 + 确认暂停。

| ID | 状态 | 任务 | 落点 / 验收 |
|---|---|---|---|
| F0.1 | ✅ | OrchestratorService += EventEmitter + `runGenerationAuto` 改后台 kickoff(非阻塞) | `orchestrator.service.ts`;起 `active_run` 立即返回 run_id |
| F0.2 | ✅ | 逐局/逐 phase 进度 emit(`match/proposal/gate/done/status`) | `orchestrator.service.ts` |
| F0.3 | ✅ | `active_run` 持久化 + 重启续接(awaiting 可续;运行中中断标 stopped) | `state.ts` + `active-run.ts` |
| F0.4 | ✅ | confirm 暂停/恢复 + `accept/reject/edited_prompt_text` | `orchestrator.service.ts` + `POST /confirm` |
| F0.5 | ✅ | `OrchestratorGateway` 桥接 socket `orchestrator.*` | `orchestrator.gateway.ts`(新) |
| F0.6 | ✅ | REST:`run-generation-auto`(非阻塞)/`stop`/`confirm`/`generations`/`versions` | `orchestrator.controller.ts` |
| F1.1 | ✅ | `/orchestrator` route + page 骨架 | `app/orchestrator/page.tsx` |
| F1.2 | ✅ | `game-client` += `orchestrator.*` 监听 + `orchestratorRun` context(合并式快照) | `lib/game-client.tsx` |
| F1.3 | ✅ | 控制面板(eval 配置 + 模式 auto/confirm + 按钮 + 状态行) | `app/orchestrator/page.tsx` |
| F1.4 | ✅ | 过程可视化面板(stepper + 双进度条 + 单局流 + champion 画像 + 提案 + 闸门) | 同上 |
| F1.5 | ✅ | 历史代列表 + 单代详情 modal | 同上;详情 modal 可回看每代 children_evaluated 的 validation/gate/holdout/失败记忆,并可从历史候选打开 diff |
| F1.6 | ✅ | 人机确认流(接受 / 拒绝 / 编辑后接受) | 同上 |
| F1.7 | ✅ | 版本面板(diff + 稳定版重激活) | 同上;版本卡可与父代对比,可将 accepted/baseline 等稳定版本设回当前 champion;candidate/rejected/champion 禁止重激活 |
| F1.8 | ✅ | 样式(复用 `iter-*` + 状态色;新增 `orchestrator.css`) | `styles/orchestrator.css` |
| F2.1 | ✅ | 大厅加"自动迭代"卡 → `/orchestrator`(复用旧 iteration 卡位) | `app/page.tsx` |
| F2.2 | ✅ | 「高级·数据接入」面板(默认折叠,7 工具) | `app/orchestrator/page.tsx`(`AdvancedDataPanel`):手动单候选 `run-generation`(M4.6)+ 单局重打分 `score`(M2)+ 校准体检 `calibration/check`(M5.11)+ 重基线 `rebaseline-plan`(M5.12)+ 场景库抽样 `scenario-bank/sample`(M6.6)+ 失败回灌 `scenario-bank/backfill`(M6.10)+ free 挖掘 `free-explore/mine`(M5.13);各工具自带表单 + 结果摘要 + 原始 JSON |
| F2.3 | ✅ | 「裁判/优化器提示词管理」section(M0.7) | 同上(`PromptManagerPanel`):`GET prompts`/`GET prompts/generations` 列示;8 资产查看改 → `POST prompts/assets` 存新版本(可激活);`POST prompts/generations` manifest_patch 组合 generation;`POST prompts/generations/activate` 激活 |
| F2.4 | ✅ | 「LLM 调用可观测」section(M0.6) | 同上(`LlmCallsPanel`):`GET llm-calls` 表格(时间/stage/模型/状态/耗时/token·cache,失败高亮)+ `DELETE llm-calls` 清空 |

> **实现顺序**:F0(后台演进,1→6 ✅)→ F1(前台,1→8 ✅)→ F2(入口 + 全端点前台入口 ✅)。全部完成。
>
> **F 已落地**:
> - F0:`OrchestratorService` 后台 runLoop emit `status/match/proposal/gate/done`;`active_run` 持久化 + 重启续接;`confirm`/`stop`;`OrchestratorGateway` 桥接 socket;REST 非阻塞 kickoff + history/versions。17 条集成断言全过。
> - F1:`/orchestrator` 页(控制面板 + 过程可视化 stepper/双进度条/单局流/提案/闸门 + 历史代详情 + 版本面板 + 稳定版重激活 + 行级 LCS diff 模态);`game-client` 消费 `orchestrator.*` + REST 动作。`next build` 通过,`/orchestrator` 路由已注册。
> - F2:大厅 iteration 卡复用为入口 → `/orchestrator`(旧 `/iteration` 已废弃);**F2.2–F2.4 补齐前台入口全覆盖**——此前只有 API、无前台触发入口的端点(手动迭代/重打分/校准/重基线/抽样/回灌/free 挖掘/prompt 管理/llm 观测)已全部接入 `/orchestrator`,经 `tsc --noEmit` + `next build` 通过。唯 `seed-baseline` 无按钮(后端每次 run/读状态自动 `seedBaselineIfMissing` 播种)。
> - **待真机验证**:前后台均已编译/构建通过 + 后台逻辑集成测试通过;端到端(socket 实时推送 + 真实对局)需起 API+web 服务 + API key 跑一代确认。新增 F2.2–F2.4 入口同样仅编译/构建验证,运行时实测(尤其依赖 DB 的 prompts 系列、依赖真人数据的校准/回灌)待起服务确认。

---

## 4. 代码基线:复用 vs 废弃

> **D1 锁定:旧 `iteration/` + `eval/prompts/` 内容全部废弃,既不复用、也不作参考来源。实现一律以新设计文档为唯一依据。**

### 4.1 直接复用(当前 live 运行时,非废弃项)

| 资产 | 位置 | 用途 |
|---|---|---|
| 模型调用封装 | `ai/ai.service.ts:callModel` | 裁判/优化器 LLM 调用(D5) |
| RNG | `sandbox/rng.ts` | 可复现随机 |
| 引擎 + MatchRecord | `sandbox/*`、`game.service.ts` | 已落地;裁判/编排器消费其 MatchRecord |
| Prompt 加载 | `ai/prompt-loader.ts` | 加载新建的裁判/优化器提示词 |

### 4.2 全部废弃(不参考、不引用、最终删除)

| 废弃项 | 位置 | 替代依据(新设计文档) |
|---|---|---|
| 旧裁判/优化器提示词 | `eval/prompts/{replay-score,auto-optimize}/*` | 《裁判提示词模板·盲测可疑度 + 诊断评分》《优化器专文 §9》 |
| 旧自迭代工具链 | `iteration/*` | 《编排器》《评分聚合》《优化器》专文 |
| 旧复盘/版本注册表 | `replay/*`、`ai/prompt-registry.ts`、`ai/eval-prompt-registry.ts` | 文件持久化(D3)+ 版本化存储(M0.4 / M5.1) |

> 实现中"提示词怎么写、聚合怎么算、循环怎么转",**只看新设计文档**,不翻旧代码。

---

## 5. 命名与落点约定（快速查阅）

```
apps/api/src/sandbox/
├── sandbox.service.ts          # 引擎单局驱动(已有)
├── match-record/               # MatchRecord 构建(已有)
├── scenario/  probe/  rng.ts   # 引擎输入侧(已有)
├── personas/                   # 侦探/填充人设(已有)
├── score/                      # 【M2 裁判】 ✅ 决策层(P1)+ 诊断层(P2)
│   ├── types.ts  objective-metrics.ts  anonymize.ts(含 atRound + buildLabeledTranscript)
│   ├── blind-suspicion.ts  veto.ts  builder.ts  score.service.ts
│   ├── per-round-trajectory.ts(M2.6)  rubric-diagnostic.ts(M2.7+2.9,含探测裁定)
│   └── multi-judge.ts(M2.11,截尾均值+judge_agreement)
├── aggregate/                  # 【M3 聚合★】 ✅ 验证信号(P1)+ 瞄准信号(P3)全完成
│   ├── types.ts(M3.1)  run-aggregate.ts(M3.2)  stats.ts(M3.5)
│   ├── pipeline.ts(M3.3/3.4/3.6/3.8)  validation.ts(M3.7/3.11)
│   └── weak-dims.ts(M3.9)  failures.ts(M3.10)
├── optimizer/                  # 【M4 优化器】 ✅ 单候选(P1)+ 多候选/会学习(P3)+ guided crossover
│   ├── input.ts(M4.1)  propose.ts(M4.3)  validate-prompt.ts(M4.4)  dedupe.ts(M4.5)
│   └── operators.ts(M4.8)  scoreboard.ts(M4.9)  assign-targets.ts(M4.7)  tried-and-rejected.ts(M4.10/4.11)
├── orchestrator/               # 【M5 编排器】 ✅ 闭环+留出+多候选+种群+校准/重基线/free API+并发调度
│   ├── prompt-version.ts(M5.1)  state.ts(M5.2,+operator_scoreboard)  generation-eval.ts(M5.8)
│   ├── gate.ts(M5.5)  holdout-eval.ts(M5.7)  paired-eval.ts(M5.3/5.4 含 cache)  orchestrator.service.ts(M5.9)  orchestrator.controller.ts
│   ├── population.ts(M5.10)  calibration.ts(M5.11)  eval-set-version.ts(M5.12)  free-explore.ts(M5.13)  scheduler.ts(M5.14)
│   └── active-run.ts  active-run/  orchestrator.gateway.ts
├── scenario-bank/              # 【M6 场景库】 ✅ 抽样器/切分/看板/台账/回灌转换链
│   └── dimensions.ts  sampler.ts(M6.6)  split.ts(M6.7)  coverage.ts(M6.9)  ledger.ts(M6.11)  replay-backfill.ts(M6.10)
└── shared/                     # 【M0 基础设施】 json-parse/store/concurrency/observability/prompt-versions

apps/api/src/ai/prompts/sandbox/
├── detective-*.txt  filler-*.txt   # 已有(对手)
├── judge/      blind-suspicion-{system,user} ✅(M2.4) / rubric-diagnostic-{system,user} ✅(M2.7)
└── optimizer/  system-prompt-optimizer.txt + user-prompt-optimizer-template.txt + crossover 专用 prompt ✅(M4)

apps/api/sandbox-out/            # 【M0.4 落盘根目录】
├── matches/   scores/   versions/   generations/   cache/
```

---

## 6. 阶段验收清单（一图速查,细节见 §2）

| Phase | 核心验收 | 涉及任务 |
|---|---|---|
| 0 ✅ | 单局跑通、MatchRecord 落盘 | M1.1–M1.9 |
| 1 ✅ | **一代闭环**:champion → 配对评测 → 优化器提案 → validate → 闸门 → 晋升/拒绝,全自动可复现 | M2 ✅ · M3 ✅ · M4.1–4.6 ✅ · M5.1–5.6/5.8/5.9 ✅ · M5.14 ✅ · M0.3–0.5 ✅ |
| 2 🚧 | 过优化集闸后,在**未见过探测**的 holdout 复核;背答案过不了 | M5.7 ✅ · M6.8 ✅ · M6.6/M6.7/M6.9 ✅ · M2.6/2.7/2.9 ✅ · 真机端到端待验 |
| 3 🚧 | 优化器不盲搜:被拒假设进记忆、算子选择随战绩收敛、recurring 被定向攻击 | M3.9/3.10 ✅ · M4.7 ✅ · M4.8/4.9/4.10 ✅ · M4.11 假设回环+crossover ✅ · 真机多代收敛待 API key |
| 4 🚧 | 代理↔真人相关性达标;种群抗局部最优;翻车能回滚 | M2.11 ✅ · M2.12 ✅ · M5.10 ✅ · M5.11 机制✅(达标需真人数据,§8) |
| 5 🚧 | 场景库由真人失败生长;holdout 验泛化;真人率下降 | M6.6/6.7/6.9/6.11 ✅ · M6.10/M5.12/M5.13 机制✅(生长需真人数据,§8) |

---

## 7. 进度维护规则

1. **每完成一个任务**:把该行 `⬜`/`🚧` 改 `✅`,并**更新 §0.1 的"当前接手点"**为下一个任务。
2. **挂起**:改 `🚧 〔挂起原因〕`,在 §0.1 注明卡在哪。
3. **新增任务**:在对应模块表内追加,编号续接;若属新模块,扩 §3。
4. **跨阶段挪动**:Phase 归属可调,但**不跳阶段**——未跑通当前 Phase 验收不进下一个。
5. **进度数字**:§1 总览表的 `x/y` 随 §3 各表更新(分子=✅ 数,分母=总数)。
6. **本文是进度单一事实来源**;代码里的 TODO/注释可指回本文对应任务 ID。

---

## 8. 真实数据验证清单（机制已建,以下需真实数据/API key 才能"验证"或"生长")

> **现状**:Phase 1–5 的【可离线搭建机制】均已实现并通过既有纯逻辑断言(累计 **173** 条);本轮补齐的 validate_prompt 强校验、guided crossover 元数据/校验、历史版本重激活、历史代详情已通过 API TypeScript 检查与前端生产构建,端到端效果仍需真实 LLM 对局确认。
> 但沙盒的设计本质是【对齐真人 + 跑真实对局】,以下能力的代码已就绪,**其"是否真有效"必须靠真实数据/真实对局确认**——这不是未实现,而是天然的外部依赖。按依赖类型分三档:

### 8.1 需 API key 跑真实 LLM 对局(引擎/裁判/优化器都是 LLM)
| 能力 | 已就绪的代码 | 真机要确认什么 | 怎么跑 |
|---|---|---|---|
| 一代闭环端到端 | 全链路已编译 | champion→评测→优化器提案→闸门→晋升/拒绝 真的跑通且落盘 | `POST /sandbox/orchestrator/run-generation-auto` `{set_id:"baseline_holdout_v1", mode:"auto"}` + `DEBUG=true` |
| 留出闸拦背答案(P2) | `holdout-eval.ts` + `baseline_holdout_v1` | 背 optimize 算术答案的改动**真的过不了** holdout(638×47 ≠ 847×293) | 同上;看 GenerationEval.child.holdout.holds=false |
| 诊断评分(P2) | `rubric-diagnostic.ts` | 八维/failure_cases/探测裁定在真实对局上产出**合理** | `scoreMatch(match,{diagnose:true})` 或诊断端点 |
| 多裁判一致度(P4) | `multi-judge.ts` | 2–3 个模型族裁判的 `judge_agreement` 真有区分度 | `scoreMatch(match,{judgeModelIds:[m1,m2,m3]})` |
| 战绩表收敛(P3) | `scoreboard.ts` + assign_targets | 跑多代后 `operator_scoreboard` win_rate 真的随战绩收敛、选靶变准 | 连续跑 N 代 auto,看 state.operator_scoreboard |
| free 模式挖掘(P5) | `free-explore.ts` | 自由对战真能发现 scripted 没有的新失败模式 | free 场景批量 runMatch + diagnose + `mineFailureCandidates` |

### 8.2 需真人对局数据(真值,沙盒里没有)
| 能力 | 已就绪的代码 | 缺的真值 | 验证标准 |
|---|---|---|---|
| 真人校准 · 相关性体检(M2.12/M5.11) | `calibration.ts` `correlationProxyVsReal` | 真人局结果(被票出/真人怀疑率)作 `CalibrationPair.real` | `correlation_proxy_vs_real ≥ 0.6`(《编排器》§13) |
| 翻车回滚(M5.11) | `shouldRollback` + `POST /sandbox/orchestrator/versions/:id/activate` | "真人说 champion 变差"的真人判定 | 沙盒说好+真人说差 → 回滚到上一稳定版 |
| 失败回灌 · 采集/闭环(M6.10) | `replay-backfill.ts`(②④⑤⑥⑦已实现) | ①真人对局全量记录(转录+票+结果);⑧下轮真人校准 | 修复后该失败模式**真人发生率下降**;台账 status→mitigated |
| 失败模式真人率(M6.11) | `ledger.ts` `real_world_rate` 字段 | 真人局中该模式致被抓比例 | 填入 real_world_rate,recurring 检测生效 |

### 8.3 需人工成稿 / 真人频率(半自动)
| 能力 | 已就绪的代码 | 缺的 | 说明 |
|---|---|---|---|
| v1 库 120 场景(M6.12) | `sampler.ts` 产 120 标签 + `split.ts` 切 + `coverage.ts` 体检 + `/sandbox/scenario-bank/sample` | 作者把标签**成稿**(roster/seed_history/台词) | 抽样器给骨架,完整 Scenario 仍需人工/回灌补 |
| 用真人频率重配比(M6.12/§5) | `dimensions.ts` 边际可换 | 真人局里各探测/局势的真实频率 | 攒够真人局后用真实频率替换先验占比 |
> **一句话**:能离线写的机制都写完了;剩下的是**把真实 LLM 对局和真人对局接进来**——那是"验证它真有效"和"让场景库真的生长"的部分,不是缺代码。先做 §8.1 的真机一代(只需 API key),再逐步接 §8.2 的真人数据。

---

> 一句话:**主链 引擎→裁判→聚合→优化器→编排器 已全部接通,Phase 1–5 机制就位;接手先读 §0.1,真实数据验证看 §8。**
